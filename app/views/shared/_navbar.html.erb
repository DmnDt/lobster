<nav class="navbar-wrapper">
  <div class="container d-flex justify-content-between align-items-center">
    <div class="navbar-links d-flex align-items-center">
      <div class="px-4"><%= image_tag "Logo.png", style: 'width: 80px' %></div>
      <div class="px-4 border-left "><%= link_to "Investisseur", projects_path(@investor) %></div>
      <div class="px-4"><%= link_to "Demandeur", new_investee_path(@investee) %></div>
    </div>
    <div>
      <div class="profil">
        <%= image_tag "icons/user-1617653.png", style: 'width: 40px', class: "avatar dropdown-toggle", data: { toggle: "dropdown" }, aria: { haspopup: "true", expanded: "false" }, id: "navbarDropdown" %>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <% if user_signed_in?%>
            <%= link_to "Sign out", destroy_user_session_path(current_user), method: :delete, class:"dropdown-item" %>
          <%else%>
            <%= link_to "Sign in", new_user_session_path,  class:"dropdown-item" %>
            <%= link_to "Sign up", new_user_registration_path,  class:"dropdown-item" %>
          <%end %>
        </div>
      </div>
    </div>
  </div>
</nav>
