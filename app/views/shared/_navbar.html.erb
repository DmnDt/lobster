<nav class="navbar-wrapper">
  <div class="navbar-container">
      <div class="navbar-links">
        <% if request.url == "#{request.base_url}/" %>
          <div class="logo">
            <%= image_tag 'Logo.png', class:"logo-lobster" %>
            <h1>Lobs<span>ter</span></h1>
          </div>
        <% else %>
          <div class="px-4"><%= link_to (image_tag "Logo.png", style: 'width: 80px'), root_path %></div>
        <% end %>
        <div class="px-4 border-left "><%= link_to "Projets", projects_path %></div>
        <div class="px-4 border-left "><%= link_to "Dashboard", dashboard_path %></div>
        <% if current_user&.investee.present? %>
          <div class="px-4"><%= link_to "Créer un Projet", new_project_path %></div>
        <% end %>
      </div>
      <div class="profil">
        <% if current_user&.investor.present? %>
          <%= image_tag "avatar/Anne.jpg", style: 'width: 40px; height: 40px; object-fit: cover;', class: "avatar dropdown-toggle", data: { toggle: "dropdown" }, aria: { haspopup: "true", expanded: "false" }, id: "navbarDropdown" %>
        <% elsif current_user&.investee.present? %>
          <%= image_tag "avatar/Oneal.jpg", style: 'width: 40px; height: 40px; object-fit: cover;', class: "avatar dropdown-toggle", data: { toggle: "dropdown" }, aria: { haspopup: "true", expanded: "false" }, id: "navbarDropdown" %>
        <% else%>
          <%= image_tag "icons/user-1617653.png", style: 'width: 40px; height: 40px; object-fit: cover;', class: "avatar dropdown-toggle", data: { toggle: "dropdown" }, aria: { haspopup: "true", expanded: "false" }, id: "navbarDropdown" %>
        <% end %>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
          <% if user_signed_in?%>
            <%= link_to "Sign out", destroy_user_session_path(current_user), method: :delete, class:"dropdown-item" %>
          <%else%>
            <%= link_to "Sign in", new_user_session_path,  class:"dropdown-item" %>
            <%= link_to "Sign up", new_user_registration_path,  class:"dropdown-item" %>
          <%end %>
        </div>
      </div>
    </div>
  </div>
</nav>
