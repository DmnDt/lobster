<%= @project.valuation %>

<%= simple_form_for @investment, url: investments_path(@investor, @project) do |f| %>
<%= f.input :location, input_html: {class: "w-100"}, wrapper_html: {class: "w-100 pr-3"} %>
<%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
